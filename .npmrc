# Force npm to use exact versions
save-exact=true

# Use package-lock.json
package-lock=true

# Increase network timeout
fetch-timeout=60000

# Disable optional dependencies for faster installs
optional=false

# Log level
loglevel=error

# Engine strict (enforce Node version from package.json)
engine-strict=true
